# Number between 1 and 100

number = float(input())

while number < 1 or number > 100: # if the number is not between those two numbers, the cycle will break and the end result will be printed.
    number = float(input())

print(f"The number {number} is between 1 and 100")
